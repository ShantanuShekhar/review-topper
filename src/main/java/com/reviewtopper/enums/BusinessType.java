package com.reviewtopper.enums;

/**
 * Generic verticals; extend as needed without schema changes (stored as string).
 */
public enum BusinessType {
    RESTAURANT,
    CAFE,
    SALON,
    CLINIC,
    GYM,
    HOTEL,
    COACHING_INSTITUTE,
    RETAIL,
    PROFESSIONAL_SERVICES,
    OTHER
}
